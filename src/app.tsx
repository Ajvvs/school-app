import React, { useState } from "react"

export const App = ({ naame }) => {
    // const [counters, setCounters] = useState<number[]>
    
    // return <h1 onClick={()=> {
    //     setCounters(counters[] + 1, ..counters)
    // }}>Hello World! Counter: {counters.join(',')}</h1>
}